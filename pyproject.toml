[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "expense_tracker"
version = "0.1.0"

[tool.setuptools]
packages = ["logic", "utility", "db_models"]